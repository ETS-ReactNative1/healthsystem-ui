import React from 'react';

const Footer = () => {
    return (
        <div style={styles.footer}>

        </div>
    );
};

const styles = {
    footer: {
        backgroundColor: "#2c3e50",
        height: 200,
        width: "100%",
        marginTop: 200
    }
}

export default Footer;